/*
  let cnt = 0;
  while(cnt < 10)
  {
    console.log("While Loop: We are at iteration "+cnt);
    cnt = cnt +1;
  }
  console.log("All done!");

  // For loop
  for(let c=0; c<10; c++)
  {
       console.log("For loop: We are at iteration "+c);
       console.log("More stuff");
  }
*/
let arr1 = ["balloon","helmet","car","bunny"];
for(let c=0; c<arr1.length; c++)
{
  console.log("We are at iteration " + c + " with the value " + arr1[c]);
}