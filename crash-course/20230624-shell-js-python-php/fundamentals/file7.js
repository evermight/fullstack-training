let iterationLimit = 10;
let cnt = 0;

while(cnt<iterationLimit)
{
  console.log("You are on iteration " + cnt);
  cnt=cnt+1;
}

for(let c=0; $c<iterationLimit; c++) {
  print("For iteration: " + c);
}